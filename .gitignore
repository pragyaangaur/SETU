__pycache__/
*.py[cod]
.venv/
venv/
.env
.DS_Store
.ipynb_checkpoints/
*.egg-info/
build/
dist/

# Downloaded science data is large and reproducible, so it is not tracked.
data/raw/
data/cache/
!data/raw/.gitkeep

# The trained weights are small and are kept in the repository, so that anyone can
# replay a storm without spending an hour retraining first.

# Correspondence and working notes. These carry names, contact details, the
# competition this was built for, and context about other work, none of which
# belongs in a public repository. The technical gaps they exist to close are
# described in the README instead.
outreach/
AGENTS.md
NOTES.md
notes/
*.private.md

# Competition material. Carries name, college, and the framing for a specific
# award, none of which belongs in a public repository.
presentation/
*.pptx
*.pdf

# The personal to do list for the competition, which is not part of the system.
CHECKLIST.md

# Local editor and tooling config
.claude/
